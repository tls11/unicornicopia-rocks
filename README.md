# unicornicopia.rocks

Static site for Unicornicopia. HTML + Tailwind + Alpine. The `public/` folder is what DigitalOcean serves.

```bash
npm install
npm run dev      # rebuild CSS on change
npm start        # serve public/
npm run build    # production CSS
```
